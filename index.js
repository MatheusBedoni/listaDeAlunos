/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ListaFlex from './ListaFlex';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ListaFlex);
